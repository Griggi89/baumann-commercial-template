---
from: cdesk
to: cl1
date: 2026-04-19
status: open
topic: Tasks queued — Property Type dropdown + Apps Script sync verify + Trial 1 follow-up
---

# Tasks for you

Chris is on the laptop with you. I've been on desktop driving demo-feedback iteration. Repo is at `0700a8c` (main, pushed). Full session context: read `from-cdesk/2026-04-19_0817_demo-feedback-session-handover.md` first — it covers everything that changed in the last batch of commits.

## Priority 1 — Property Type dropdown on CF template (Chris approved)

**Why:** During the /demo feedback session Chris asked "is that somewhere on the CF sheet? cause we might add that via a drop down so there's no discrepancy." He approved the dropdown idea ("love the drop down"). Currently Property Type is a free-text cell in the CF Settings tab and prone to typos/drift.

**What to build:**
- Add a Google Sheets **data validation** rule on the Property Type cell in the CF template Settings tab.
- Enum options (propose these, confirm with Chris if you want):
  - `Office`
  - `Retail`
  - `Industrial Showroom`
  - `Industrial / Warehouse`
  - `Medical`
  - `Hospitality`
  - `Mixed Use`
  - `Service Station`
  - `Childcare`
- **Where:** the CF template at sheet ID `1VlUOAJhNSFpMLauT3Sq2CG1ORpuBa2-8RJIoAOSy0aA` (Settings tab) — so all future deals inherit it.
- **How to apply:** Apps Script route is cleanest. Add a function in `apps-script/` (new file, e.g. `04_seed_dropdowns.gs`, or inline in `01_master_index_app.gs`) that installs the validation on a given sheet. Expose it as a menu item "BPI Commercial → Seed Dropdowns" so Chris can run it on the template and on existing deal CF sheets.
- Alternative: one-off install via a standalone script run once against the template — future deals cloned from the template inherit the rule automatically.

**Downstream:** no populator or dashboard changes needed. `03_populator.gs` already copies the cell value as-is, `fetchSheetData.ts` reads it verbatim into `features.details`, dashboard renders it. The dropdown only prevents typos upstream.

## Priority 2 — Verify Deal Manager modal change

Commit `0700a8c` changed the Deal Manager from `showSidebar(420px)` to `showModalDialog(620×700)` — so it pops front-and-center like the residential one (Chris wanted UX parity).

**Apps Script is not auto-synced.** Chris needs to re-paste `01_master_index_app.gs` into the bound Apps Script project (or `clasp push`). When he does that:
1. Ask him to click **BPI Commercial → Deal Manager** in the Master Index sheet
2. Confirm it opens as a centered modal (not a right sidebar)
3. Confirm all 3 steps (Create DD / Run Pipeline / Get Dashboard URL) are visible without scrolling

If width/height needs tweaking, update `01_master_index_app.gs:71-78` and re-push.

## Priority 3 — Trial 1 status

Per your handover from 2026-04-18, Trial 1 (99 Marine Parade Redcliffe) was queued. I didn't run it — I was working on `/demo` only. When Chris is ready:
- CF sheet values are still at template defaults per your notes — he can either paste real Mon Komo numbers or run as-is for a smoke test
- Dashboard URL should resolve: https://baumann-commercial-template.vercel.app/deals/99-marine-parade-redcliffe-qld-4020
- Watch for: `Valuation` row in Settings tab (new field I added — see handover note data-model section). If missing, it defaults to 0 and the row hides cleanly. If you want to populate it, add a `Valuation` label to the CF Settings tab mapping.

## Data-model change to be aware of (IMPORTANT)

`PropertyData.cashflow.upfrontCosts.valuation: number` is now a required field. Impact:
- `lib/fetchSheetData.ts:159-169` reads `toNum(s['Valuation'])` from Settings
- `defaultPropertyData` sets it to 0
- Populator does NOT yet write this label — if Chris's CF Calc has a "Valuation cost" row (per his screenshot, row 11 at $3,000), the populator needs to mirror that into Settings with label `Valuation`

**Action:** check `03_populator.gs` for the Settings mirror mapping. If it doesn't already forward "Valuation cost" → `Valuation`, add it.

## Not for you

- `/demo` and the commercial dashboard UI — I've been iterating there, don't touch unless I flag something
- New `.claude/launch.json` in my working copy — local-only, gitignored

## How to reach me

I've got a 5-min cron polling this repo. Whatever you push to `main` or drop in `from-cl1/` gets surfaced to Chris on desktop within ~5 min. Direct me to Chris if you need a human click.

— CDesk
