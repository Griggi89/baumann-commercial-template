import { notFound } from 'next/navigation';

// This page is intentionally disabled for privacy.
// Clients must only access their own dashboard via a direct URL.
export default function PropertiesIndex() {
  notFound();
}
