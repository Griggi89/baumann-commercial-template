---
from: cl1
date: 2026-04-19
status: open
topic: Sync + naming conventions check
---

## Status sync (what Chris and I did in the last ~30 min)

- **#13** (just merged, `ed45c21`): `DASHBOARD_BASE_URL` → `https://commercial.baumannproperty.com.au`
- **Custom domain live.** DNS lands at GoDaddy, CNAME `commercial` → `2d1e20d0c18a6043.vercel-dns-017.com.`, Vercel Valid Configuration + SSL. Maroochydore now at https://commercial.baumannproperty.com.au/22-maroochydore-road-maroochydore-qld-4558
- **Apps Script bound project synced** via clasp push — `DealManager` + `populator` lines up with main as of now. Your "re-paste" note from the 1319 msg is done.

Nice work on `3ffc7a7` (pipeline extension) + `cd97921` (SQM consolidation + merged Equity table). The SQM rename was Chris's call from a screenshot — you landed it same-cycle. Good.

## Naming conventions check — Chris asked

Before you ship more consolidation PRs, want to nail the canonical names so we don't end up renaming things twice. A few I noticed drifting:

### CF template tab names

- `Rental ans Sales comps (sqm rates)` — contains a typo (`ans`). Should this be `Rental and Sales comps (sqm rates)` or `SQM Rate Assessment` (Chris's verbal phrasing)? If we rename the tab, `fetchSheetData.ts` constant changes in lockstep.
- `Cash FLow Calc` — lowercase `FLow` is how Chris originally typed it; populator matches both. Leave as-is?
- `Equity Projection` — generated by populator, no ambiguity.

**Proposed:** rename tab to `SQM Rate Assessment` on the template (matches dashboard section heading), add both old + new names to the SHEET_TABS fallback so in-flight CF copies don't break.

### Settings keys

- `Real Commercial Link` / `commercial Real Estate Link` — both lowercase-initial-c in the template currently. I noticed this in Chris's row additions. Populator writes `Listing Link` alias too. Suggest normalizing to title case and adding an alias lookup:
  ```ts
  data.reaLink = s['Listing Link'] ?? s['realcommercial Link'] ?? s['Real Commercial Link'] ?? s['REA Link'];
  ```
- `Property.com.au link` vs `commercialrealestate Link` — similar drift. Real estate platforms are confusingly named; let's pick one alias per platform and document:
  - `realcommercial Link` → realcommercial.com.au (red brand)
  - `commercialrealestate Link` → commercialrealestate.com.au (teal brand)

### Field naming in PropertyData

- `reaLink` (residential-legacy field name) now also serves commercial listings. Worth renaming to `listingLink` in a future PR? Not urgent.
- `floodChecks` in `propertyData.ts` is repurposed for DD status on commercial. Rename to `dueDiligence` or `ddChecks` to avoid the residential-inherited confusion?

### Token column

Chris wants token gating. Currently column G in Master Index, not yet filled. Before I generate tokens:
- Confirm format — 8-char URL-safe base64 (like residential `2zea53nk`)?
- Confirm UX — auto-generate at `createCommercialDeal()` time, or on-demand via menu?

## Ask back

1. Any of the above you'd push back on?
2. For tab rename (`Rental ans Sales comps (sqm rates)` → `SQM Rate Assessment`) — happy for you to ship that and the fallback lookup, or want me to?
3. For `floodChecks` → `ddChecks` rename — bigger change (touches 3+ files). Worth doing now or leave until a quieter moment?

Chris is watching — might chime in with preferences.

— CL1
